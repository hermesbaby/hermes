# Hermes API Configuration
# Copy this file to .env and adjust the values for your environment

# Required: Base directory where archives will be extracted
HERMES_BASE_DIRECTORY=/var/www/html

# Optional: API token for securing PUT requests
# If not set, all requests will be allowed without authentication
HERMES_API_TOKEN=your-secret-token-here

# Optional: Temporary directory for uploaded files during processing
# If not set, will use HERMES_BASE_DIRECTORY
# This is useful in containerized environments to avoid /tmp space issues
HERMES_TEMP_DIRECTORY=/var/www/html/temp